f1 = 200;
fs = 500;
n = 0:50;
t = n/fs;
a = sin(2*pi*f1*t);
stem(t,a)
%---------------
f_con = 10000;
end_n_con = floor(max(n)/fs*f_con);
n_con = 0:end_n_con;
t_con = n_con/f_con;
a_con = sin(2*pi*f1*t_con);
hold on 
plot(t_con,a_con)
%---------------
n = 0:50;
t = n/fs;
a = sin(2*pi*f1*t);
stem(t,a)